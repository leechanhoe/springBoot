Shift + f6 누르면 한번에 여러개 바꾸기 가능
get set 생성 alt + insert
ctr + alt + v 리턴값에 따라 객체만들어줌
ctr + alt + Shift + t    refactor - 따로떼서 함수같은것으로 만들어줌
alt + enter    static으로 넘김
ctr + e    최근 조회 파일목록
alt를 option이라고도함
ctr + alt + <-   이전 동작으로 가기
ctr + alt + N     a = b(a);    return a; 이걸 인라인으로 합쳐줌